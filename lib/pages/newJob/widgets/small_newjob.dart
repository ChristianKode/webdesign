import 'package:flutter/material.dart';

class SmallNewJob extends StatelessWidget {
  const SmallNewJob({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}